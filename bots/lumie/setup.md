LUMA_API_KEY is set in the environment. Prove it by listing the user's events through the API.
Browser automation is available to the agent (the Claude in Chrome extension is connected) and the browser is signed in to Luma, Gmail and LinkedIn. Ask the user to confirm the sign-ins and wait.
